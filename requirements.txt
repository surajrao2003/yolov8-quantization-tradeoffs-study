# Install after the steps in README "Environment setup":
#   PyTorch (pytorch.org), onnxruntime-gpu[cuda,cudnn]==1.23.2, conda tensorrt + pycuda,
#   pip tensorflow, then this file.
#
# Download Ultralytics checkpoints: models/yolo_models/ (e.g. yolov8n.pt)
#
# Instructions for Jetson deployment :- 
# Jetson Orin Nano (aarch64, L4T): use JetPack’s TensorRT and PyCUDA; do not use the desktop
# conda TensorRT lines from the README. After system packages are set, pip install this file
# for NumPy, OpenCV, PyYAML, etc. where arm64 wheels exist. Run tensorrt_benchmarking/trt_inference_jetson.py on the board.

numpy>=1.23.0
pandas>=2.0.0
matplotlib>=3.7.0
tabulate>=0.9.0
opencv-contrib-python>=4.8.0
PyYAML>=6.0
ultralytics>=8.1.0
onnx>=1.14.0
